<?xml version="1.0" encoding="utf-8"?>

<app:FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/switch_layout"
    android:layout_width="150dp"
    android:layout_height="50dp"
    android:layout_marginTop="60dp"
    android:layout_marginLeft="70dp"
    app:layout_constraintLeft_toRightOf="@id/gl_test"
    app:layout_constraintTop_toBottomOf="@+id/tv_test_sub">

<LinearLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <Switch
        android:id="@+id/labeledSwitch"
        android:layout_width="match_parent"
        android:layout_height="25dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:switchMinWidth="25dp"
        android:thumb="@drawable/thumb"
        android:track="@drawable/track" />

</LinearLayout>

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvClose"
        android:layout_width="117dp"
        android:layout_height="75dp"
        android:layout_weight="0.5"
        android:gravity="center"
        android:text="Close"
        android:textColor="@color/red"
        android:textSize="15dp"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/tvOpen"
        android:layout_width="0dp"
        android:layout_weight="0.5"
        android:gravity="center"
        android:visibility="invisible"
        android:layout_height="match_parent"
        android:text="Open"
        android:textColor="@color/green"/>
</LinearLayout>
</app:FrameLayout>